name: calculator
description: A flutter calculator.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  fonts:
   - family: Segment7
     fonts:
       - asset: assets/fonts/Segment7Standard.otf
         weight: 500
